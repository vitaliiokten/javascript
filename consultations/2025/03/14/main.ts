const getCylinderArea = (h: number, r: number): number => 2 * Math.PI * r * h;

// В цьому коді IntelliJ пропонує визначати типи саме так (в двух місцях), як по мені то це здається зайвим. Чи дійсно так й треба?"
